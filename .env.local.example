# When running the app locally, create a new file named .env.local in the root directory,
# copy the contents of this file into it, and then fill in the values with your
# Firebase project's credentials.

# 1. For client-side Firebase (found in Firebase Console > Project settings > General)
NEXT_PUBLIC_FIREBASE_API_KEY="YOUR_API_KEY"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="YOUR_PROJECT_ID.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="YOUR_PROJECT_ID"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="YOUR_PROJECT_ID.appspot.com"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="YOUR_MESSAGING_SENDER_ID"
NEXT_PUBLIC_FIREBASE_APP_ID="YOUR_APP_ID"

# 2. For server-side Firebase Admin (found in Firebase Console > Project settings > Service accounts > Generate new private key)
# IMPORTANT: The value should be the entire JSON object, enclosed in quotes.
FIREBASE_SERVICE_ACCOUNT_KEY='{"type": "service_account", "project_id": "...", ...}'
